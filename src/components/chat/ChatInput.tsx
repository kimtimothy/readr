interface ChatInputProps {
  isDisabled?: boolean;
}

const ChatInput = ({ isDisabled }: ChatInputProps) => {
  return <div className="flex-1 justify-between flex flex-col mb-28"></div>;
};

export default ChatInput;
